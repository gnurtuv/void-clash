// js/main.js
window.addEventListener('load', function() {
    const game = new Game('gameCanvas');
    // Game is initialized by its constructor, including setting up event listeners.
    // The game loop will start when the "Start Game" button is clicked.
});